/**
 * Public deployment settings.
 *
 * The site automatically derives the repository URL when it runs on a
 * github.io project site. These values provide sensible local-preview and
 * pre-deployment fallbacks. Add marketplace URLs when the listings are live.
 */
window.REDLINE_SITE_CONFIG = Object.freeze({
  repositoryUrl: "https://github.com/TheAnarchoX/redline-support",
  chromeStoreUrl: "",
  firefoxAddonsUrl: "",
});
